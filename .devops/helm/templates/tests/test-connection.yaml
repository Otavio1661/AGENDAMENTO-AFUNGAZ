apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unigazin.fullname" . }}-test-connection"
  labels:
    {{- include "unigazin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unigazin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
